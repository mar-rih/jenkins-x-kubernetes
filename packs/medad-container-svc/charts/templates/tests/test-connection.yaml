apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "md-container-rest.fullname" . }}-test-connection"
  labels:
{{ include "md-container-rest.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "md-container-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
